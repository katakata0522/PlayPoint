# 第4回の本番未反映とテスト証跡の補完

基準: a9191f2f4de9fbbf1a9d354147bdfaefe5b97153（PR #343）。

## 確認できた不足

Deploy run 35274726631 / job 105382423081 は全回帰でCHECK_FAILとなり、SSH・本番転送より前に停止した。PR Gateと同じtreeでも、同じ実行結果を保証しない。個別assertionは当初取得できず、原因を一時的失敗と断定していない。

既存preflightは子プロセスの出力を端末へ継承するが、preflight.jsonに保存するのは工程の結果だけである。Deployの保管済み成果物には個別の失敗test名・assertionが残らず、ジョブログ取得が失敗すると原因調査が止まる。

## 最小の補完

Node標準の複数reporterで、一回のテスト実行を端末と既存のCI証跡ディレクトリの両方へ出力する。全回帰はregression.tap、圧縮後はpost-minify-tests.tapに分け、後者で前者を上書きしない。既存always()の成果物保管・保持7日をそのまま利用し、workflow・job・権限・依存を増やさない。環境変数全体の記録やSSH工程の出力保存は行わない。

テスト集合・実行回数・順序・失敗伝播・性能基準を変えない。通常のpreflightは資産を復元し、--prepare-deployだけ配信用変換を保持する。UI・計算式・記事・保存形式・Consentには変更しない。

## 検証の所有

主担当は引き続きtests/preflight-execution-contract.test.cjs。既存の6ケースを維持し、隔離した小さなfixtureを実Nodeで実行する4ケースを追加する。本リポジトリの全テストを再帰的に実行しない。

- 通常モード／配信用モードで、端末と保存TAPの一致、全回帰と圧縮後の分離、実行回数を確認。
- 意図的にassertionを失敗させ、失敗名と理由が保存され、後続工程が成功しても全体が失敗することを確認。
- 保存先をディレクトリにして実I/Oを失敗させ、成功扱いしないことを確認。

ローカルNode v22.16.0では10/10成功、skip/cancelなし。CIの固定Nodeによる全体検査は当該PRの成果物で別途確認する。個別監査の基準930中80確認済み・850未確認は増やさない。新規4ケースと基準ケースのレビュー件数を混同しない。

## 未完了の扱い

この変更は記録不足の修正であり、run 35274726631の元のテスト失敗原因を特定したという意味ではない。最新候補で同じ失敗が出た場合は保存TAPから原因を特定して修正する。成功した場合も、履歴上の失敗原因まで解明したとは報告しない。本番HTTP・exact SHA・verified・Chromiumは通常Deployの実結果で確認する。
