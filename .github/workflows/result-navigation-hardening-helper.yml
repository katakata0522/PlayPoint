name: Result navigation hardening helper

on:
  push:
    branches:
      - fix/result-navigation-hardening

permissions:
  contents: write

jobs:
  harden:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo paused
