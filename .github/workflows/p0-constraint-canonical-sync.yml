name: P0 Constraint Canonical Sync

on:
  push:
    branches:
      - fix/p0-remove-arbitrary-content-constraints-20260918
    paths:
      - ".github/workflows/p0-constraint-canonical-sync.yml"

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6.0.2
        with:
          ref: fix/p0-remove-arbitrary-content-constraints-20260918

      - name: Set up pinned CI Node
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.0.1
        with:
          node-version-file: .github/ci-runtime/node-version
          package-manager-cache: false

      - name: Generate canonical outputs
        run: node scripts/prepare-pr.cjs

      - name: Commit generated outputs
        run: |
          set -euo pipefail
          if git diff --quiet; then
            echo "Canonical outputs already match."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "build: sync P0 constraint outputs"
          git push origin HEAD:fix/p0-remove-arbitrary-content-constraints-20260918
